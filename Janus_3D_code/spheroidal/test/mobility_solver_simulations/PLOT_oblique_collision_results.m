% Plot saved checkpoints from TEST_oblique_collision.m.
%
% Set result_source to the exact checkpoint file you want to replay.

this_dir = fileparts(mfilename('fullpath'));
repo_root = fileparts(fileparts(fileparts(this_dir)));
result_source = fullfile(repo_root, 'Oblique collision.mat');

opts = struct( ...
    'replay', true, ...
    'frameStep', 1, ...
    'pauseTime', 0.01, ...
    'summaryPlots', false, ...
    'saveAnimation', true, ...
    'animationFormat', 'gif', ...
    'animationPath', fullfile(repo_root, 'oblique_collision'), ...
    'frameRate', 30, ...
    'plotTrajectories', true, ...
    'plotTrajMaxPoints', 400, ...
    'plotSurfaceAlpha', 0.5, ...
    'plotGrid', true, ...
    'plotColor', 'mu', ...
    'plotColorMode', 'inf', ...
    'plotColormap', 'turbo', ...
    'plotForceVectors', true, ...
    'plotView', [0 15], ...
    'plotTitle', 'Oblique collision', ...
    'plotShowTimestep', true ...
);

plot_saved_mobility_results(result_source, opts);
